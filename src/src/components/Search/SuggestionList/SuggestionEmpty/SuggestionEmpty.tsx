import './SuggestionEmpty.scss';
import {ClipLoader} from "react-spinners";

export const SuggestionEmpty = () => {
    return <div key={'load'} className={'suggestion-empty'}>
        Sin resultados
    </div>
}